# 引き継ぎメモ：実動時間計測 × 鬼速PDCA

最終更新: 2026-06-30（セッション終了）  
関連リポジトリ:
- 鬼速PDCA: `Challenge 3_鬼速PDCA-app`（本ファイル）
- ジャーナリング(GAS): `Challenge 2_self-report-app/Code.gs`

---

## 完了したこと

- **3点セット確立**
  - 計画 = Google Calendar（`[BtoB]` 等の接頭辞）
  - 実動 = LINE ボタン → `time_log` シート
  - 振り返り = 鬼速PDCA（Check / Adjust）
- **GAS ジャーナリングアプリ**
  - `time_log` シート（1記録=1行）
  - Flex 2タップ（カテゴリ → 30/60/90分）— Phase A ✅
  - Rich Menu（⏱ 実動 / 📊 サマリー）— Phase C ✅
  - テキスト `BtoB 90` は案内のみ（実動はボタン経由）
- **ドライラン完了・本番運用開始**
- **日曜 Plan スマホ再開（A+B）** — ローカル `Code.gs` 実装済み。**GAS へのコピペ反映は未**（次回）

---

## 確定した運用ルール

| ツール | 役割 | タイミング |
|--------|------|-----------|
| Google Calendar | 計画（`[BtoB]` 等） | 週初・スケ組み |
| LINE ⏱ 実動 | 実動 → `time_log` | 作業の切り替え時 |
| LINE クイズ | 習慣・歩数・できたこと等 → `log` | 朝・昼・夜（Push → そのまま回答） |
| 鬼速PDCA | 週次 Check / Adjust | **日曜**（先週の日〜土を振り返り） |

**週の区切り:** 日曜始まり・土曜終わり（PDCA `getWeekStart()` と同じ）

**5カテゴリ統一:**

| カテゴリ | カレンダー | LINE |
|----------|-----------|------|
| BtoC | `[BtoC]` | BtoC |
| BtoB | `[BtoB]` | BtoB |
| BtoG | `[BtoG]` | BtoG |
| スポット | `[Spot]` | Spot |
| レバレッジ | `[Lev]` | Lev |

**毎週水曜は休日**（`[BtoB]` 定例なしは正常。比較時に欠けとして扱わない）

**接頭辞なしの予定** — 仕事っぽい予定でも `[BtoB]` 等を付けなくてよい（5カテゴリ比較対象外）

**日曜 Plan スマホ操作** — `予算` で再開、`予算スキップ` で中断。午後クイズ完了後に Plan 未完了なら自動リマインド

---

## フェーズ整理

| Phase | 名称 | 状態 |
|-------|------|------|
| 0 | 習慣・運用の検証 | ✅ 完了 |
| A | 実動入力ボタン化（Flex） | ✅ 完了 |
| C | Rich Menu | ✅ 完了 |
| **運用** | 本番データ収集 1〜2週 | 🔄 **いまここ** |
| 整備 | カレンダー `[BtoB]` 単語登録整理 | 🔜 並行可 |
| B | 混線防止（ガード） | ⏸ 任意・保留可 |
| 1 | PDCA 週次タイム配分表 | 🔜 2週目以降 |

---

## Phase B について（重要な判断）

- **当初名:** ジャーナル混線防止（📝メモボタン）
- **本当の問題:** 意図しないテキストが H列ジャーナル等に入る誤送信（`BtoB 90` 事故があった）
- **ユーザー判断:** H列への日中自由メモは使わない（夜クイズの ⑧⑨ で足りる）
- **結論:** 📝 メモボタンは **不要**。誤送信が再発したら **ガードのみ** 検討。今は **スキップ可**

---

## 次にやること

### 次回やること（優先）

1. **GAS 反映** — `Challenge 2_self-report-app/Code.gs` を Apps Script にコピペ保存
2. LINE で `予算` / `予算スキップ` の動作確認
3. **7/5（日）** — 外出日: 午後クイズ後に Plan リマインドが来る想定

### 今週（運用フェーズ）

**日次（30秒）**
- ⏱ 実動を記録したか
- カレンダー `[BtoB]` と LINE カテゴリが一致しているか

**日曜（振り返り）**
- PDCA で先週（日〜土）の Check
- `time_log` とカレンダーを見比べ、計画 vs 実動の差をメモ

**観測メモ（1行）**
```
日付 / 問題 / 対応要否
```

### 並行
- `[BtoB]` 等の接頭辞・単語登録の表記整理

### 来週日曜
- 1週目振り返り後 → Phase B 要否 / Phase 1 着手を判断

### 2週目以降
- **Phase 1:** PDCA `ReviewPane` に週次タイム配分表（計画h vs 実動h）
- データ源: カレンダー + GAS `time_log`

---

## 新チャット用コピペ

```
【コンテキスト】
実動時間計測 × 鬼速PDCA の本番運用中。
詳細は Challenge 3_鬼速PDCA-app/docs/HANDOFF-time-pdca.md を読んでください。

【完了】
Phase 0/A/C、time_log + Flex + Rich Menu、本番運用開始

【いま】
1〜2週の実データ運用（観測フェーズ）

【次】
- 並行: [BtoB] カレンダー接頭辞整理
- Phase B: スキップ可（誤送信再発時のみガード検討）
- Phase 1: PDCA 週次タイム配分表（2週目以降）

【確定事項】
- 週: 日曜〜土曜
- 毎週水曜は休日（BtoB 定例なしは正常）
- 接頭辞なしの仕事っぽい予定はそのままでよい
- Plan: 「予算」で再開、「予算スキップ」で中断
- Phase B の 📝 メモは不要（夜クイズで足りる）
- GAS: Challenge 2_self-report-app/Code.gs
```

---

## 主要ファイル

| ファイル | 内容 |
|----------|------|
| `Challenge 2_self-report-app/Code.gs` | LINE Webhook, time_log, クイズ, Rich Menu |
| `Challenge 3_鬼速PDCA-app/src/lib/utils.ts` | `getWeekStart()` 日曜始まり |
| `Challenge 3_鬼速PDCA-app/src/components/ReviewPane.tsx` | Phase 1 追加候補 |

---

## 本番 URL

- PDCA: https://oni-pdca-app.vercel.app
